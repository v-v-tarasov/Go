/* Исполняемая программа, использует библиотеку fmt и содержит функцию
   main. Функция не имеет аргументов, ни чего не возвращает, использует
   функцию Println из библиотеки fmt и вызывает ее, передавая аргумент:
   строку "Hello, World!" на стандартный вывод в терминал. */

package main

import "fmt"

func main() {
	fmt.Println("Hello, World!")
}
